import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  box: {
    alignItems: "center",
    borderWidth: 1,
    margin: 20,
    borderRadius: 50,
  },
  categoryTitle: {
    fontSize: 30,
  },
});
