import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
  },
  styleButton: {
    width: 250,
  },
  textError: {
    textAlign: "center",
    fontSize: 40,
    marginTop: 70,
  },
});
